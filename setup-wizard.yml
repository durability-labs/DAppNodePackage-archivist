version: "2"
fields:
  - id: MODE
    target:
      type: environment
      name: MODE
      service: archivist-node
    title: Archivist mode
    description: >-
      - **`Archivist node`** - useful for local testing/development and basic/files sharing.

      - **`Archivist node with marketplace support`** (`Recommended`) - you can share files and buy the storage, this is the main mode and should be used by the end users.

      - **`Archivist storage node`** - should be used by storage providers or if you would like to sell your local storage.
    enum:
      - archivist-node
      - archivist-node-with-marketplace
      - archivist-storage-node
    required: true

  - id: NETWORK
    target:
      type: environment
      name: NETWORK
      service: [archivist-node, geth]
    title: Archivist network
    description: >-
      - **`Testnet`** - Archivist Testnet network. Check the [docs](https://docs.archivist.storage/networks/networks) for available networks.
    enum:
      - testnet
    required: true

  - id: ETH_PRIVATE_KEY
    target:
      type: environment
      name: ETH_PRIVATE_KEY
      service: archivist-node
    title: ETH_PRIVATE_KEY
    description: >-
      Ethereum private key which will be saved to a file which will be passed as a value for `ARCHIVIST_ETH_PRIVATE_KEY` variable.
    secret: true
    pattern: "^0x[a-fA-F0-9]{64}$"
    patternErrorMessage: Must be a valid private key (0x1fd16a...)
    required: true
    if: { "MODE": { "enum": ["archivist-node-with-marketplace", "archivist-storage-node"] } }

  - id: ARCHIVIST_ETH_PROVIDER
    target:
      type: environment
      name: ARCHIVIST_ETH_PROVIDER
      service: archivist-node
    title: ARCHIVIST_ETH_PROVIDER
    description: >-
      The URL of the JSON-RPC API of the Ethereum node - use [Public RPC endpoint](https://docs.archivist.storage/networks/networks) or local Geth node RPC.
    pattern: >-
      ^(http(s|)\:\/\/|)((([a-zA-Z0-9-_]{1,}\.){1,})([a-zA-Z]{1}[a-zA-Z0-9-]{1,}))(:[0-9]{1,}|)(\/[a-zA-Z0-9_~#?\+\&\.\/-=%-]{1,}|)$
    patternErrorMessage: Must be a valid URL
    required: true
    if: { "MODE": { "enum": ["archivist-node-with-marketplace", "archivist-storage-node"] } }

  # We can't make 'required' conditional and need to define save variable two times
  - id: ARCHIVIST_MARKETPLACE_ADDRESS-node-with-marketplace-support
    target:
      type: environment
      name: ARCHIVIST_MARKETPLACE_ADDRESS
      service: archivist-node
    title: ARCHIVIST_MARKETPLACE_ADDRESS
    description: >-
      Address of deployed Marketplace contract.

      We should set this variable **only if we would like to override auto-detected address**. Check the address for the network you are using in the [docs](https://docs.archivist.storage/networks/networks).
    pattern: "^0x[a-fA-F0-9]{40}$"
    patternErrorMessage: Must be a valid Ethereum address
    required: false
    if: { "MODE": { "enum": ["archivist-node-with-marketplace"] } }

  - id: ARCHIVIST_MARKETPLACE_ADDRESS-storage-node
    target:
      type: environment
      name: ARCHIVIST_MARKETPLACE_ADDRESS
      service: archivist-node
    title: ARCHIVIST_MARKETPLACE_ADDRESS
    description: >-
      Address of deployed Marketplace contract.

      We should set this variable because we are running in **Archivist `storage node`** mode. Check the address for the network you are using in the [docs](https://docs.archivist.storage/networks/networks).
    pattern: "^0x[a-fA-F0-9]{40}$"
    patternErrorMessage: Must be a valid Ethereum address
    required: true
    if: { "MODE": { "enum": ["archivist-storage-node"] } }

  - id: IP_MODE
    target:
      type: environment
      name: IP_MODE
      service: archivist-node
    title: IP detection mode
    description: >-
      Set your Public IP address manually or use autodetection.
    enum:
      - auto
      - manual
    required: true

  - id: ARCHIVIST_NAT
    target:
      type: environment
      name: ARCHIVIST_NAT
      service: archivist-node
    title: ARCHIVIST_NAT
    description: >-
      Specify method to use for determining public address. Must be one of: `any`, `none`, `upnp`, `pmp`, `extip:<IP>`, default = `any`. Check the [docs](https://docs.archivist.storage/learn/run#nat-configuration) for more details.
    pattern: "^(any|none|upnp|pmp|extip:((25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]?|0).){3}(25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]?|0))$"
    patternErrorMessage: Must be a valid IP address
    required: true
    if: { "IP_MODE": { "enum": ["manual"] } }

  - id: NAT_PUBLIC_IP_AUTO
    target:
      type: environment
      name: NAT_PUBLIC_IP_AUTO
      service: archivist-node
    title: NAT_PUBLIC_IP_AUTO
    description: >-
      IP Lookup Service URL to be used to Public IP address autodetect, default = [`https://ip.archivist.storage`](https://ip.archivist.storage).
    pattern: >-
      ^(http(s|)\:\/\/|)((([a-zA-Z0-9-_]{1,}\.){1,})([a-zA-Z]{1}[a-zA-Z0-9-]{1,}))(:[0-9]{1,}|)(\/[a-zA-Z0-9_~#?\+\&\.\/-=%-]{1,}|)$
    patternErrorMessage: Must be a valid URL
    required: true
    if: { "IP_MODE": { "enum": ["auto"] } }

  # - id: ARCHIVIST_METRICS
  #   target:
  #     type: environment
  #     name: ARCHIVIST_METRICS
  #     service: archivist-node
  #   title: ARCHIVIST_METRICS
  #   description: >-
  #     Enable the metrics server, default = `false`.
  #   enum:
  #     - "true"
  #     - "false"
  #   required: false

  - id: ARCHIVIST_STORAGE_QUOTA
    target:
      type: environment
      name: ARCHIVIST_STORAGE_QUOTA
      service: archivist-node
    title: ARCHIVIST_STORAGE_QUOTA
    description: >-
      The size of the total storage quota dedicated to the node, default = `8gb`. Accept common units like kb, mb, gb, tb.
    pattern: "^[0-9A-Za-z]+$"
    patternErrorMessage: Must be a valid storage quota value - number with an optional unit
    required: true

  - id: ARCHIVIST_BLOCK_TTL
    target:
      type: environment
      name: ARCHIVIST_BLOCK_TTL
      service: archivist-node
    title: ARCHIVIST_BLOCK_TTL
    description: >-
      Default block timeout in seconds - 0 disables the ttl, default `24h`. Accept common units like s, h, d, w.
    pattern: "^[0-9A-Za-z]+$"
    patternErrorMessage: Must be a valid block timeout - number with an optional unit
    required: true

  - id: ARCHIVIST_LOG_LEVEL
    target:
      type: environment
      name: ARCHIVIST_LOG_LEVEL
      service: archivist-node
    title: ARCHIVIST_LOG_LEVEL
    description: >-
      Sets the log level, default = `info`. Available options: `info`, `warn`, `error`, `debug`, `trace`. Check the [docs](https://docs.archivist.storage/learn/run#logging) for more details.
    pattern: "^[A-Za-z-,;:]+$"
    patternErrorMessage: Must be a valid log level
    required: true

  - id: ARCHIVIST_API_PORT
    target:
      type: environment
      name: ARCHIVIST_API_PORT
      service: archivist-node
    title: ARCHIVIST_API_PORT
    description: >-
      The REST API port, default = `8080`.
    pattern: "^((6553[0-5])|(655[0-2][0-9])|(65[0-4][0-9]{2})|(6[0-4][0-9]{3})|([1-5][0-9]{4})|([0-5]{0,5})|([0][0-9]{1,4})|([0-9]{1,4}))$"
    patternErrorMessage: Must be a valid port number
    required: false

  - id: ARCHIVIST_DISC_PORT
    target:
      type: environment
      name: ARCHIVIST_DISC_PORT
      service: archivist-node
    title: ARCHIVIST_DISC_PORT
    description: >-
      Discovery (UDP) port, default = `8090`.
    pattern: "^((6553[0-5])|(655[0-2][0-9])|(65[0-4][0-9]{2})|(6[0-4][0-9]{3})|([1-5][0-9]{4})|([0-5]{0,5})|([0][0-9]{1,4})|([0-9]{1,4}))$"
    patternErrorMessage: Must be a valid port number
    required: false

  - id: EXTRA_OPTS
    target:
      type: environment
      name: EXTRA_OPTS
      service: archivist-node
    title: EXTRA_OPTS
    description: >-
      Extra options to pass to the Archivist node. Check the [docs](https://docs.archivist.storage/learn/run#cli-options) for more details.
    pattern: "^.*$"
    patternErrorMessage: Must be a valid string
    required: false

  - id: portMapping_ARCHIVIST_LISTEN_ADDRS
    target:
      type: portMapping
      containerPort: 8070/TCP
      service: archivist-node
    title: Port mapping for Archivist Node - ARCHIVIST_LISTEN_ADDRS
    description: >-
      Transport (TCP) port, default = `8070`.
    pattern: "^((6553[0-5])|(655[0-2][0-9])|(65[0-4][0-9]{2})|(6[0-4][0-9]{3})|([1-5][0-9]{4})|([0-5]{0,5})|([0][0-9]{1,4})|([0-9]{1,4}))$"
    patternErrorMessage: Must be a valid port number
    required: true

  - id: portMapping_ARCHIVIST_DISC_PORT
    target:
      type: portMapping
      containerPort: 8090/UDP
      service: archivist-node
    title: Port mapping for Archivist Node - ARCHIVIST_DISC_PORT
    description: >-
      Discovery (UDP) port, default = `8090`.
    pattern: "^((6553[0-5])|(655[0-2][0-9])|(65[0-4][0-9]{2})|(6[0-4][0-9]{3})|([1-5][0-9]{4})|([0-5]{0,5})|([0][0-9]{1,4})|([0-9]{1,4}))$"
    patternErrorMessage: Must be a valid port number
    required: true
